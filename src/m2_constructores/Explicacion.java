package m2_constructores;

/*
 *¿Qué son los CONSTRCUTORES en Programación Orientada a Objetos (OOP)?
Un constructor es un método especial que se utiliza para inicializar un objeto cuando
se crea a partir de una clase. Es el primer bloque de código que se ejecuta cuando se 
instancia un objeto y permite asignar valores iniciales a los atributos del objeto o 
realizar configuraciones necesarias.

Características de los Constructores:
Mismo Nombre que la Clase:

El constructor debe tener el mismo nombre que la clase.
Esto permite identificarlo como un método especial para inicialización.
Sin Tipo de Retorno:

No tiene un tipo de retorno (ni siquiera void), porque su único propósito es inicializar 
el objeto.Se Invoca Automáticamente:

Se ejecuta automáticamente cuando se crea un objeto utilizando la palabra clave new.
Sobrecarga de Constructores:

Una clase puede tener múltiples constructores con diferentes parámetros. 
Esto se conoce como sobrecarga, y permite flexibilidad al crear objetos con distintas 
configuraciones.Uso de this y super:

this se utiliza para referirse al objeto actual y diferenciar atributos de parámetros.
super se utiliza para invocar el constructor de la clase padre en una jerarquía de herencia.
 
 * 
 * 
 * 
 * */
 
