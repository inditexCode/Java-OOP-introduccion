package m8_abstracto;

/*
 *En Java, una clase abstracta es una clase que no se puede instanciar directamente. 
 *Su propósito es servir como una base para otras clases, proporcionando una estructura 
 *común y, a menudo, parcialmente implementada. Las clases abstractas son útiles cuando 
 *quieres definir un comportamiento común que debe ser compartido por múltiples clases, 
 *pero que también necesitan personalizar detalles específicos. 
 * 
 * */
